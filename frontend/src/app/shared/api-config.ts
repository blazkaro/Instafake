export const apiConfig = {
    production: false,
    baseUrl: '/api',
}

export enum ApiPaths {
    Auth = '/auth',
    Posts = '/posts',
    Multimedia = '/multimedia',
    Likes = '/likes',
    Profiles = '/profiles',
    Follows = '/follows',
    Notifications = '/notifications'
}